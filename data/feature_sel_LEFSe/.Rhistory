library(ggpubr)
library(ggplot2)
data<-read.csv("selected_features.csv")
data
data$group<-factor(data$group)
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "desc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
library(ggpubr)
library(ggplot2)
data<-read.csv("selected_features.csv")
data$group<-factor(data$group)
png("plot.png",width = 12,height = 5,res = 300,units="in")
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "desc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
dev.off()
png("plot.png",width = 8,height = 5,res = 300,units="in")
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "desc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
dev.off()
library(ggpubr)
library(ggplot2)
data<-read.csv("selected_features.csv")
data$group<-factor(data$group)
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "desc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
library(ggpubr)
library(ggplot2)
data<-read.csv("selected_features.csv")
data$group<-factor(data$group)
png("plot.png",width = 8,height = 5,res = 300,units="in")
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "desc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
dev.off()
png("plot.png",width = 8,height = 8,res = 300,units="in")
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "desc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
dev.off()
library(ggpubr)
library(ggplot2)
data<-read.csv("selected_microbes.csv")
data$group<-factor(data$group)
data
png("plot.png",width = 8,height = 8,res = 300,units="in")
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "desc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
dev.off()
library(ggpubr)
library(ggplot2)
data<-read.csv("selected_microbes.csv")
data$group<-factor(data$group)
png("plot.png",width = 8,height = 8,res = 300,units="in")
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "desc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
dev.off()
png("plot.png",width = 8,height = 8,res = 300,units="in")
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "asc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
dev.off()
png("plot.png",width = 8,height = 9,res = 300,units="in")
ggbarplot(data, x = "Features", y = "value",
fill = "group",           # change fill color by mpg_level
color = "black",            # Set bar border colors to white
palette = "jco",            # jco journal color palett. see ?ggpar
sort.val = "asc",          # Sort the value in descending order
sort.by.groups = TRUE,     # Don't sort inside each group
x.text.angle = 0,          # Rotate vertically x axis texts
ylab = "LDA Score",
legend.title = "Clusters",
rotate = TRUE
)+scale_y_continuous(expand = expansion(mult = c(0, .1)))
dev.off()
data<-read.csv("to_lefse.csv",sep="\t",index_col=1)
data<-read.csv("to_lefse.csv",sep="\t",row.names =1)
View(data)
for (i in 1:245){
t<-wilcox.test(data[,i]~data$ExacerbatorState)
print(t$p.value)
}
wilcox.test()
?wilcox.test()
for (i in 1:245){
t<-wilcox.test(data[,i]~data$ExacerbatorState)
if(t$p.value<=0.1){
print(colnames(data)[i])
}
}
nam<-c()
for (i in 1:245){
t<-wilcox.test(data[,i]~data$ExacerbatorState)
if(t$p.value<=0.1){
nam<-c(nam,colnames(data)[i])
}
}
nam
